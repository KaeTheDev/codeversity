import React from 'react';
import { Alert } from 'reactstrap';

const SiteMessage = () => (
    <div>
        <Alert id = "alert">
            <div><b>Try it free 7 days</b> then $20 / month thereafter</div>
        </Alert>
    </div>
);

export default SiteMessage;